export default function HundredIcon({ title }: { title?: string }) {
  return (
    <img
      src="/icons/100.png"
      alt="steps"
      style={{ filter: 'invert(1)', height: '25px' }}
      title={title}
    />
  )
}
